[pytest]
# Tests import as `backend.…`, so the repository root must be on sys.path.
pythonpath = .
testpaths = backend/tests
python_files = test_*.py
addopts = -ra --strict-markers
asyncio_mode = strict

markers =
    integration: needs a live database and a running backend; skipped automatically in CI
